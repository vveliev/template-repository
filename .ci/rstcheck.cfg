[rstcheck]
report_level=WORNING
ignore_language=rst
ignore_messages=(Duplicate (ex|im)plicit target.*|Hyperlink target ".*" is not referenced\.$)
additional_dependencies=[sphinx]
ignore_directives =
    automodule,
    click,
    mdinclude,
    spelling,
    mermaid,
